fix(rtgamma): DICOM座標整合の厳密化とZスライス順序修正、README更新

- io_dicom: GFOV(Gr idFrameOffsetVector)に基づきフレーム配列を昇順再配置し、Z座標と線量配列の不一致を解消
- main: IPP差を参照の方向余弦（row/col/slice）へ射影して原点補正を厳密化
- main: 探索最適シフト（参照軸成分）をLPSベクトルへ変換してからリサンプリングに適用
- main: 出力先ディレクトリが空文字の場合に作成しないようガード
- docs: READMEを全面更新（使い方、引数、出力例、最近の修正を追加）
- docs: prompt3.txtの文字化けを修正し、内容を明確化

背景:
自己比較は100%で安定。異グリッド/スケール差が大きい組み合わせでは低パス率となるが、座標整合の厳密化でソフト側の不確かさを低減。

影響:
IOP/IPP/PixelSpacing/GFOVの任意組み合わせでも一貫した座標整合が向上。既存の引数・I/Fは後方互換。
