# Progress and handoff record — 2026-08-13

This record closes the 2026-08-13 development and TPS-workstation test session. It documents the reviewed source state, the unreleased offline candidate, manual findings, and the safe restart point. It contains no DICOM input, UID, GUI configuration, or workstation-specific input/output path.

## Repository and pull-request state

- Active branch: `fix/allow-frame-of-reference-override`.
- Reviewed branch head: `78cb44aaae81d67aae5609360f40fac232f4afb5` (`fix(offline): preserve Unicode Python paths`).
- The branch and `origin/fix/allow-frame-of-reference-override` pointed to the same reviewed commit before this handoff file was added.
- `main` and `origin/main` point to `a8421335b48c0344ef2ce8c9303f799eb6b5eb16`, the documentation-only handoff commit after v0.9.4.
- The published `v0.9.4` tag still points to `9ecd7ad8159f1b93af087028e87cf0bd0df4451c`. Do not move it.
- There is one Git worktree and no stash entry.
- The feature branch was clean before this handoff file was added. Publishing this record creates a documentation-only commit after the previously reviewed PR head, so CI and Codex review must run again.
- Draft PR [#29](https://github.com/inata169/GPR-comparing/pull/29) is open, targets `main`, and is reported mergeable. It contains 2 commits, 22 changed files, 326 additions, and 34 deletions.
- CI run 159 completed successfully for head `78cb44a`.
- The final Codex re-review covered `78cb44a`, found no major issues, and left no review thread. PR #29 remains draft and has not been merged.

## Source changes completed today

### Explicit Frame of Reference UID override

Commit `a3d397b44054851f80bd06fd1aa8902bda76128f` adds an explicit, safety-scoped override for a verified anonymization-only `FrameOfReferenceUID` mismatch.

- Default behavior remains fail-closed.
- CLI and batch require the explicit `--allow-frame-of-reference-mismatch` option.
- Source and packaged GUI launchers expose a checkbox and require confirmation for each launch.
- Both Viewer launch paths accept the same explicit setting.
- The setting and warning are recorded in report/cache provenance.
- The override does not perform registration, alignment, or an inferred shift. It only permits comparison using the existing absolute DICOM patient-coordinate geometry.
- It must not be used when the doses belong to unrelated studies or genuinely different coordinate frames.

### Offline installer Unicode-path repair

Commit `78cb44aaae81d67aae5609360f40fac232f4afb5` fixes the Japanese/non-ASCII external-Python path regression.

- `cmd.exe` no longer reads the selected UTF-8 Python path through `set /p`.
- PowerShell invokes the selected external Python directly to create the application-dedicated virtual environment.
- The batch installer uses only bundle-local paths after virtual-environment creation.
- The external Python installation, global packages, PATH, and registry are not intentionally modified.
- A regression test covers a Japanese virtual-environment path; the focused offline installer/preflight set recorded 11 passing tests.

## Offline candidate and automated evidence

The clean unreleased candidate built from `78cb44a` is retained locally at:

`dist/uid-override-78cb44a/offline/GPR-comparing-offline-win64-py312.zip`

- SHA-256: `92c74c13296b6fbca97579933e8f763a2df50b629c81e0b21726406e70ea1e96`
- Size: `230,934,929` bytes
- The installed acceptance copy is retained in a local sibling directory outside this repository.
- Bundle verification covered 243 files, 25 wheels, the third-party license material, the CPython signature/hash, offline-only pip installation, PyMedPhys 0.41.0, Numba, and offscreen Qt imports.
- The rebuilt candidate installed successfully through an existing external CPython 3.12 x64 path containing Japanese characters, without the earlier mojibake failure.
- Installer/smoke evidence covers Header Compare, 2D PyMedPhys PDF/PNG generation, synthetic 3D PyMedPhys, and explicit 3D Numba execution.

This ZIP is an unreleased PR #29 acceptance candidate. It is not the exact v0.9.4 source and must not be named or distributed as the completed v0.9.4 binary bundle. It contains post-v0.9.4 fixes. No EXE build was performed after the project owner explicitly deferred EXE work until a separate request.

## Manual TPS-workstation findings

### Confirmed

- The repaired offline installer completed on the workstation and created the dedicated application virtual environment.
- The explicit UID override reaches the installed CLI from the GUI. With the override selected, geometry validation continued with a prominent warning and the 3D calculation started.
- Prior Viewer failure returned exit code 1, a GUI error dialog, and a captured Viewer launch log. This provides partial evidence for abnormal-process error propagation.
- The application currently has no working GPU calculation backend. `--gpu` is provenance-only and reports `gpu_applied=false`; the absence of a GPU is not the cause of the observed PyMedPhys runtime.

### Representative full-volume run did not complete

The final installed-candidate attempt used:

- engine: PyMedPhys 0.41.0
- global Gamma, 3% dose difference, 2 mm DTA, 10% cutoff
- `global_max` normalization
- interpolation fraction 5
- shift optimization off
- explicit verified Frame of Reference UID override
- 53,051 cutoff-qualified reference points out of 1,438,560 grid points

PyMedPhys continued emitting 30-second heartbeats through approximately 2,731 seconds (45 minutes 31 seconds) and was then canceled by the user. There was no traceback after calculation started, so elapsed time alone is not considered a functional crash. The representative `run3d` reports, `gamma3d.npz`, and `diff3d.npz` were not generated by this canceled run.

The long runtime is explained by the current reference-engine contract: GUI `Threads` is not applied to PyMedPhys, interpolation fraction 5 creates a dense search, passing points are not exited early, and the maximum Gamma search is unbounded. A many-core Xeon therefore does not imply a sub-minute PyMedPhys result.

### Later TPS-PC completion result

The project owner subsequently confirmed that the offline candidate installed successfully on the TPS PC and that a full-volume PyMedPhys run completed with:

- engine: PyMedPhys (reported by the operator as "Mrdphys")
- interpolation fraction: 1
- GUI Threads setting: 18; this setting is not applied by the PyMedPhys engine
- elapsed time: 5 minutes 14 seconds
- reported GPR: 0.02%

This establishes successful installation and PyMedPhys workflow completion at interpolation fraction 1. It does not establish an acceptable dose-comparison result: the near-zero GPR is currently attributed to a known PHITS-calculation defect in the separate `dicomxphits` GUI, for which a fix is in progress. Record this as integration evidence only and repeat the representative acceptance calculation after `dicomxphits` produces corrected evaluation dose data. Do not diagnose the 0.02% result as a GPR-comparing defect from this evidence alone.

## Required next manual acceptance steps

Start with the fast CPU-parallel Numba path; do not repeat the same full-volume PyMedPhys run first.

1. Launch the installed candidate and verify the two selected inputs belong to the same intended anatomy/coordinate system before allowing the UID mismatch.
2. Run representative 3D Gamma using this fixed initial acceptance protocol:
   - engine: `Numba (fast full-volume GPR)`
   - DD/DTA/cutoff: `3% / 2 mm / 10%`
   - normalization: `global_max`
   - interpolation fraction: `4`
   - threads: `0` (automatic)
   - shift optimization: off
   - explicit UID override: on only after verification
3. Record elapsed time, effective engine, interpolation fraction, applied thread behavior, pass rate, and the complete GUI process log.
4. Confirm generation of JSON, Markdown, PDF, chart output, `gamma3d.npz`, `diff3d.npz`, and SQLite data.
5. Launch the Fast 3D Viewer and inspect Ref Dose, Eval Dose, Dose Diff, Dose Ratio, Gamma, and Pass/Fail.
6. Use a new empty output directory to check dose-only fallback without deleting or overwriting the successful Gamma cache.
7. Retain the prior error-propagation evidence and, if another safe Viewer input failure occurs naturally, confirm that the GUI again reports the exit code and exact log location.

Do not change interpolation fraction case by case to obtain a preferred GPR. If another value is adopted, define and record it as a fixed protocol before comparison.

## Remaining project decisions after manual acceptance

1. Review this handoff file and decide whether to commit it to PR #29 or preserve it in a later documentation-only commit.
2. If the Numba Gamma/Viewer workflow passes, mark PR #29 ready only after the project owner confirms that the manual evidence is sufficient.
3. Re-run `@codex review` and CI if any source or PR-scoped documentation changes are pushed after reviewed head `78cb44a`.
4. Merge PR #29 only after review, CI, and the agreed manual acceptance gate pass.
5. Do not retrofit the fixes into the published v0.9.4 tag. A new release/version decision is required if these fixes are to be distributed.
6. After an exact release source/tag is approved, rebuild the final offline ZIP from that exact source and repeat integrity, licensing, `--no-index`, installation, representative Numba Gamma, reports, and Viewer checks.
7. Do not build an EXE unless the project owner explicitly requests it.

## Deferred performance ideas — no implementation started

The following ideas were discussed only; they require a separate specification, validation plan, branch, PR, and acceptance cycle:

- a PyMedPhys GPR-only mode using pass-based early exit;
- a finite and explicitly reported `max_gamma` performance limit;
- process/block parallelization of the PyMedPhys reference workload;
- cutoff-aware bounding-box or ROI-limited calculation with sufficient DTA margin;
- a validated GPU backend and offline dependency plan.

Numba remains the practical CPU-parallel full-volume GPR path. Its early exit preserves its sampled pass/fail decision but does not produce minimum passing Gamma values numerically equivalent to the PyMedPhys reference engine.

## Local-only evidence and privacy boundary

The candidate ZIP, installed bundle, DICOM inputs, GUI configuration, run logs, Viewer logs, reports, PDFs, charts, SQLite databases, NPZ caches, and workstation-specific paths remain local and Git-ignored. Do not add them to Git. The final long-run log is saved locally as `run_log_20260813_151353.txt` for troubleshooting but must be privacy-reviewed before any publication.

## End-of-session state and restart procedure

- No GPR-comparing GUI, Gamma, or Viewer process was running at closeout.
- No process needed to be forcibly terminated.
- PR #29 remains draft and unmerged.
- No tag, release, external Python installation, or published asset was changed during closeout.

At the next session, begin without switching branches blindly:

```powershell
git status --short --branch
git branch --show-current
git log -3 --oneline --decorate
git stash list
git worktree list
git fetch origin
git rev-parse "v0.9.4^{commit}"
```

Read `AGENTS.md`, `docs/PROGRESS_2026-08-12.md`, and this file. Then inspect PR #29 through the GitHub connector, confirm that reviewed head `78cb44a` and CI run 159 are still current, and continue with the Numba manual acceptance run above.
