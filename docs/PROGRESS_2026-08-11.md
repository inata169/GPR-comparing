# Progress record — 2026-08-11

## PyMedPhys standardization checkpoint

The source target is CPython 3.12 with PyMedPhys 0.41.0 as the standard gamma engine. The explicit Numba path remains available for legacy and research comparisons. The full local `tests/` suite completed with `99 passed, 7 skipped, 8 warnings`, and Ruff completed without findings.

The controlled full-volume 5 x 5 cm source-GUI and Fast 3D Viewer checks are recorded separately in [PyMedPhys controlled RTDOSE verification record — 2026-08-11](PYMEDPHYS_CONTROLLED_RTDOSE_VERIFICATION_2026-08-11.md).

## Offline bundle candidate

| Item | Value |
|---|---|
| Bundle | `GPR-comparing-offline-win64-py312.zip` |
| Local build location | `dist/pymp/` (Git-ignored) |
| Size | 230,905,483 bytes |
| SHA-256 | `f1d0ee2af6508b3c61d1590d83f65e818b96e07cb0e2d4248cb1d97c9ccc7048` |
| Source revision recorded in bundle | `6584d22ed63ccb68d2fe0accc133623c707b61e6-dirty-validation-only` |
| Python payload | CPython 3.12.10, Windows x64 |
| Wheel count | 25 |
| Built UTC | 2026-08-11T04:39:04Z |

The build created a fresh temporary Python 3.12 virtual environment, installed the pinned runtime exclusively with `pip --no-index --find-links`, and verified the runtime, PyMedPhys 0.41.0, Numba, DICOM/report dependencies, PySide6, and PyQtGraph. The final bundle checksum manifest and third-party licensing coverage checks passed.

A subsequent smoke run from the candidate bundle tree passed these checks:

- Python 3.12 x64 and runtime imports;
- Fast Viewer dependency import;
- generated non-patient CT, RTDOSE, and RTSTRUCT loading;
- RTDOSE header comparison;
- 2D PyMedPhys 0.41.0 Gamma with JSON, Markdown, PDF, and PNG outputs;
- 3D PyMedPhys 0.41.0 Gamma;
- explicit 3D Numba legacy Gamma.

## Limitations and pending acceptance

- The ZIP was built from a worktree explicitly marked `dirty-validation-only`; it is a validation candidate, not an approved release artifact.
- `--no-index` installation verification demonstrates that the wheelhouse is self-contained for the exercised environment, but the PC itself was not physically disconnected from the network.
- No Windows x64 PC without Python is currently available. Clean-machine first-install acceptance remains pending by project-owner decision.
- No version, tag, GitHub Release, or clinical-use approval is implied by this record.
- Source/derived DICOM and generated case results remain local and are not included in the bundle or repository.
