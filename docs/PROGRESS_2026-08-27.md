# Progress and handoff record — 2026-08-27

This record closes the ROI/DVH reliability fix, fixed-condition source-workflow verification, v0.9.6 release preparation, and local candidate build. It contains no DICOM UID, patient information, workstation-specific input path, GUI configuration, or generated Gamma output.

## Repository and release state

- PR #34 was squash-merged as `765a1485f7e9b440af295235146302f3533ca7f8`. It prevents non-finite evaluation-dose ROI values from aborting a completed Gamma run, uses common paired finite support for reference/evaluation DVHs, and slices dose volumes and masks consistently in fast 2D axial, sagittal, and coronal analysis.
- PR #35 prepared v0.9.6 release documentation and was squash-merged as `cbc1b6cadf145b27ce659a020192e0c550bf264d` after Codex review returned no finding and all six CI jobs passed.
- The annotated `v0.9.6` tag points to `cbc1b6cadf145b27ce659a020192e0c550bf264d` and was pushed after the candidate archives passed local verification.
- The published `v0.9.4` and `v0.9.5` tags remain unchanged at `9ecd7ad8159f1b93af087028e87cf0bd0df4451c` and `74881d77ee8d654cbe2c368e457136cbf6827da2`, respectively.
- GitHub Release publication and binary-asset upload were not performed. They require a separate publication decision.

## Fixed-condition source-workflow verification

The corrected evaluation RTDOSE was used with Frame of Reference mismatch permission disabled. The two controlled conditions were kept separate:

| Purpose | Engine | Interpolation fraction | Elapsed time | GPR | Evaluated points |
| --- | --- | ---: | ---: | ---: | ---: |
| Reference calculation | PyMedPhys 0.41.0 | 1 | 19.918 s | 62.6431% | 178,136 |
| Practical fast GPR | Numba 0.65.1 | 4 | 23.297 s | 92.4833% | 178,136 |

Both used 3% dose difference, 2 mm distance to agreement, 10% cutoff, global/global-max normalization, and shift optimization off. The engine maps and statistics are not treated as numerically equivalent, and no case-specific interpolation tuning was performed.

JSON, CSV, Markdown, PDF, charts, SQLite, `gamma3d.npz`, and `diff3d.npz` were generated. The cache contract and report schema were validated. Ref Dose, Eval Dose, Dose Diff, Dose Ratio, Gamma, and Pass/Fail rendered from the validated cache. Dose-only fallback and abnormal Viewer-exit reporting were also checked.

This is software functional evidence only. It is not clinical QA, patient-dose approval, commissioning, a medical-device claim, or a clinical GPR threshold.

## v0.9.6 candidate build

Both archives were built from a clean detached worktree at the exact `v0.9.6` tag using a dedicated temporary CPython 3.12.10 x64 environment. The EXE build used PyInstaller 6.22.2. The temporary process environment alone selected the dedicated Python and marked the detached worktree as safe for Git identity inspection. No persistent PATH, registry, external Python installation, global package, or GUI configuration was changed.

Verified local candidates retained under the Git-ignored `dist/v0.9.6-release` label are:

| Artifact | Size | SHA-256 |
| --- | ---: | --- |
| `GPR-comparing-v0.9.6-exe-fast-win64.zip` | 446,977,020 bytes | `7f43cce8b739a5d11f726e2828beac4fc3f11c2930c5e9f5f6ecc8b70b36f9cb` |
| `GPR-comparing-v0.9.6-offline-win64-py312.zip` | 230,959,042 bytes | `6a0806dc765f6f593ecc9e2069f39f3255f1ef4653f6ed65deb6027670714356` |

These are local release candidates, not published GitHub Release assets. Do not rename an earlier ZIP to these names and do not replace existing v0.9.4 or v0.9.5 assets.

## Package verification completed

- ZIP CRC verification passed for all 6,991 EXE ZIP entries and all 308 offline ZIP entries.
- EXE package identities at the package root, CLI, standard Viewer, and Fast Viewer all record version `0.9.6`, commit `cbc1b6cadf145b27ce659a020192e0c550bf264d`, and `git_dirty=false`.
- The offline application identity records version `v0.9.6`, the same commit, and `git_dirty=false`.
- The Fast manifest contains one `qwindows.dll` in a Qt platform-plugin path, no configured GPL-only Qt review target, and no PySide6/Qt component in the non-Fast CLI or standard Viewer folders.
- The EXE ZIP contains the CLI, standard Viewer, Fast Viewer, manifest, notices, and collected third-party license material.
- The offline ZIP contains the SHA-256 and signature-verified official CPython 3.12.10 x64 installer, 25 fixed wheels, third-party licensing material, and 250 immutable files covered by its internal checksum manifest.
- Offline-only installation into a temporary verification environment completed without an index. PyMedPhys 0.41.0, Numba 0.66.0, PySide6 Essentials 6.11.1, PyQtGraph 0.14.0, and the remaining pinned runtime set imported successfully.
- The CLI, standard Viewer, and Fast Viewer executables returned exit code zero for noninteractive startup/help checks.
- The extracted CLI completed synthetic, non-patient 3D Gamma runs with PyMedPhys 0.41.0 / interpolation fraction 1 and Numba 0.66.0 / interpolation fraction 4. Both produced 100% over 23,848 evaluated points, valid ROI output, report files, SQLite, and bound NPZ caches with application version `0.9.6` and the tagged commit.
- The extracted Fast Viewer remained running with the synthetic CT, RTDOSE, RTSTRUCT, report, and validated Numba cache until the automated startup check ended it.

The packaged Numba version follows the tracked v0.9.6 offline constraints. It differs from the Numba 0.65.1 source environment used for the fixed-condition workflow record; neither result is used to claim cross-engine or cross-version numerical equality.

## TPS-PC operational check scope

The project owner approved changing the next TPS-PC step from a second formal packaged acceptance study to a **release-candidate EXE operational check**. Use the exact hash-verified `GPR-comparing-v0.9.6-exe-fast-win64.zip` candidate rather than an older EXE or offline ZIP.

1. Verify the transferred ZIP SHA-256 against this record.
2. Extract it to a short writable local folder and start `run_gui_fast_exe.bat`.
3. Confirm the GUI reports application version 0.9.6 and opens without an unexpected error.
4. Execute one representative 3D Gamma workflow using the already approved input pair and prospectively fixed settings. Do not tune interpolation to obtain a preferred GPR.
5. Open the saved result in the Fast Viewer and confirm that the expected dose and Gamma overlays are available.
6. Confirm that the report and cache files are saved and that no unexpected GUI or Viewer error appears.

This operational check focuses on the transferred EXE, Windows security/runtime behavior, file access, GUI launch, and saved-result handoff. It does not repeat the source-workflow study, define a clinical threshold, or approve clinical use. The offline ZIP does not need to be installed merely to execute this EXE check.

## Privacy and artifact boundary

Keep DICOM inputs, source/evaluation hashes tied to governed local data, GUI configuration, workstation paths, logs, reports, charts, screenshots, NPZ, PDF, SQLite, extracted packages, and ZIP files outside Git. Only the privacy-conscious version, commit, fixed settings, summary results, artifact sizes, SHA-256 values, and verification outcomes above belong in this record.

After the TPS-PC operational check is reported, decide separately whether to publish a GitHub Release and upload these exact hash-verified assets. Do not rebuild or silently replace an asset under the same filename after publication.
